<% if (title) { %>
    <!-- Content Heading: Displays the title passed from the controller -->
    <h1><%= title %></h1>
  <% } else {
    res.redirect('/')
  } %>

  <%# messages() %>

  <%- grid %>
